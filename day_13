统计


mean()                 %平均值

median()               %从小到大排序后，中位数的值。若中位数有两个，则取两个数的平均值

mode()                 %出现次数最多的数

prctile()              %将数值从小到大排序后，取第百分之多少的数。如：prctile(A,25);prctile(A,75)%按照个数分为4段，取25%（q1）与75%（q3）处的数值（50%（q2）处的数值可由median算出）

max()/min()            %最大/最小值，由此可算出数据的大小与范围

var()                  %概率论数据统计中的样本方差（二阶中心矩）

std()                  %样本标准差

统计绘图有时需要将数据统计出现次数，此时可用命令：
histgram()/tabulate()
histgram直接做出柱状图（x轴为数值，y轴为出现次数）。
tabulate则是将数据整理输出三列，第一列是各元素排序，第二列是各元素出现次数，第三列是各元素所占百分比


推论统计（假设检验）

看以后有没有学习要求
